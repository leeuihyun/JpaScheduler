spring:
  application:
    name: hyun

  datasource:
    url: jdbc:mysql://localhost:3306/jpascheduler
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect